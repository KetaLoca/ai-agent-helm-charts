{{- if .Values.config.fromFiles -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "openclaw-instance.configMapName" . }}
  labels:
    {{- include "openclaw-instance.labels" . | nindent 4 }}
data:
  {{- toYaml .Values.config.fromFiles | nindent 2 }}
{{- end }}
